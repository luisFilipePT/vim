const x = 1;

for (let i = 0; i < 10; i++) {
  console.log('hello');
  if (i === 5) {
    console.log('another indent level');
  }
}

let b;

// Test comment
const c = null;

const arr = [1, 2, 3];

const res = arr.reduce((previous, current) => previous + current, 0);

console.log(res);
